{
  "name": "qyshare-keeper",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "wrangler pages dev dist",
    "deploy": "vite build && wrangler pages deploy dist"
  },
  "dependencies": {
    "hono": "^4"
  },
  "devDependencies": {
    "@hono/vite-cloudflare-pages": "^1",
    "@hono/vite-dev-server": "^0",
    "@cloudflare/workers-types": "^4",
    "vite": "^6",
    "wrangler": "^4"
  }
}
